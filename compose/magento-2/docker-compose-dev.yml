version: "3"

services:
  app:
    volumes: &appvolumes
      - magento222_appdata:/var/www/html:nocopy
      - ~/.composer:/var/www/.composer

  phpfpm:
    volumes: *appvolumes

  db:
    volumes:
      - dbdata:/var/lib/mysql

volumes:
  dbdata:
  magento222_appdata:
    external: true
