services:
  app:
    volumes: &appvolumes
      - ./src/nginx.conf.sample:/var/www/html/nginx.conf:cached

  phpfpm:
    volumes: *appvolumes
    environment:
      - DB_PASS=root@123
  ssh:
    image: markoshust/ssh
    ports:
      - "22:22"
    volumes: *appvolumes
