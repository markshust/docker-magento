#!/bin/bash
[ $# -eq 0 ] && echo "Usage: ${0} command" && exit
__dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
docker-compose -f "${__dir}/../docker-compose.yml" -f "${__dir}/../docker-compose.dev.yml" exec phpfpm "$@"
